name: rani-ukamble-group/reusable_files_application
jobs:
  call-build:
    uses: Target-orgn/target-simple-reusable1/.github/workflows/build_file.yml@main
    with:
      environment: ${{ inputs.environment }}
  call-test:
    uses: Target-orgn/target-simple-reusable1/.github/workflows/test_file.yml@main
    with:
      environment: ${{ inputs.environment }}
