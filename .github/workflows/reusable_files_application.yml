name: rani-ukamble-group/reusable_files_application
true:
  workflow_dispatch:
    inputs:
      environment:
        description: Environment to deploy to
        required: true
        default: production
jobs:
  build:
    uses: Target-orgn/reusable2-post/.github/workflows/build_file.yml@post5
    with:
      environment: ${{ inputs.environment }}
  test:
    uses: Target-orgn/reusable2-post/.github/workflows/test_file.yml@post5
    with:
      environment: ${{ inputs.environment }}
